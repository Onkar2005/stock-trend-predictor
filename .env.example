# Copy this file to `.env` and fill in your real values.
# Never commit the actual `.env` file (it's already in .gitignore).
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stock_trend_db
DB_USER=root
DB_PASSWORD=yourpassword
